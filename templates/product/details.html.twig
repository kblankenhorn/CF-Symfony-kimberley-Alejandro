{% extends 'base.html.twig' %}

{% block body %}
<div class="row">
<div class="col-12">
<div class="card">
                <img src="{{ product.image}}" class="card-img-top img-thumbnail" alt="...">
                <div class="card-body">
                    <h5 class="card-title">Travel Options</h5>
                    <ul class="text-info">
                      
                        <li>{{ product.name }}</li>
                        <li>{{ product.price }}</li>
                        <li>{{ product.description}}</li>
                </ul>
             
                    <a href="/" class="btn btn-info">Return to Main</a>
                </div>
            </div>
</div>
  
  </div> 
{% endblock %}